@model MPR.Models.MeatSports

<table class="table" id="example">
    <tbody>
        @foreach (var sport in Model.Sports)
        {
            <tr><td colspan="5" class="text-success text-capitalize font-weight-bold">@sport.Name</td></tr>
            foreach (var game in sport.Games)
            {
                <tr>
                    <td class="text-left">@game.AwayTeam</td>
                    @if (@game.NotifyAway)
                    {
                        <td style="font-weight: bold" class="text-success">@game.AwayTeamScore</td>
                    }
                    else
                    {
                        <td>@game.AwayTeamScore</td>
                    }

                    <td class="text-left">@game.HomeTeam</td>
                    @if (@game.NotifyHome)
                    {
                        <td style="font-weight: bold" class="text-success">@game.HomeTeamScore</td>
                    }
                    else
                    {
                        <td>@game.HomeTeamScore</td>
                    }

                    <td><a href="@game.TimeLink">@game.Time</a></td>
                </tr>
            }
        }
    </tbody>
</table>
